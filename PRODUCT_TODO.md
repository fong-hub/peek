# Peek 产品待办清单

这份清单从产品迭代角度整理，目标不是单纯“多加功能”，而是把 Peek 从“偶尔打开看一下文件”推进到“愿意长期作为开发者文件浏览器使用”。

## 当前判断

- 当前最清晰的产品定位是：开发者文本文件预览器。
- 当前最需要补的是留存和浏览深度，不是盲目扩格式。
- 当前不建议优先做：插件系统、冷门格式支持、复杂高级定制。

## 重构判断（2026-07）

- 不建议整体重写。Tauri、React、Zustand 和预览器路由仍适合当前产品规模，构建与测试链路也稳定。
- 已完成必要的局部重构：单文件状态升级为页签会话；HTML 预览改为 DOM 级资源处理；本地 ES module 使用可追踪的缓存依赖图。
- 下一阶段只在功能推动时继续拆分：加入前进/后退和搜索前，将 Store 拆为 `workspace`、`tabs`、`preferences` 三个领域；预览格式继续保持独立 Previewer。
- 暂不引入插件架构或重写 Rust 主进程。它们不会直接改善当前的切换效率与预览准确性，迁移风险高于收益。

## 迭代原则

- 先修正用户预期，再扩能力边界。
- 先做高频使用链路，再做低频展示功能。
- 先提升复用率和停留时长，再追求“支持更多格式”。

## P0: 先把产品说清楚

- [ ] 明确定位文案
  - 统一 README、关于弹窗、空状态、官网文案。
  - 二选一：`开发者文件预览器` 或 `通用文件预览器`。
  - 如果暂时不支持图片、PDF、SVG，就不要给用户通用预览器预期。

- [ ] 修正文档和实际能力不一致的问题
  - README 中写到的 `JSON 折叠树` 需要补齐，或者先改成 `格式化展示`。
  - 检查所有“已支持/已实现”文案，避免承诺过度。

- [ ] 明确“支持边界”
  - 在空状态和不支持预览页明确说明：当前优先支持 Markdown、JSON、HTML、日志、代码、纯文本。
  - 对图片、PDF、Office、二进制文件给出清晰反馈，不让用户误以为是异常。

## P1: 提升再次打开和持续使用的概率

- [ ] 把空状态改成“继续使用入口”
  - 增加最近打开列表。
  - 增加“恢复上次工作区”。
  - 增加“固定常用项目/文件夹”。
  - 目标：应用打开后 1 次点击内回到上一次工作上下文。

- [ ] 持久化工作状态
  - 记住上次打开的文件夹。
  - 记住上次选中的文件。
  - 记住主题、侧边栏宽度、信息面板开关。
  - 目标：关闭再打开时，用户能无缝续上。

- [ ] 增强最近使用能力
  - 最近记录按文件和文件夹分组展示。
  - 支持清空全部、固定、移除失效路径。
  - 对失效路径给出“文件不存在”反馈，而不是静默失败。

- [ ] 增加 CLI 打开能力
  - 支持 `peek <file>` 和 `peek <folder>`。
  - 目标：让 Peek 融入开发者命令行工作流。

## P1: 补上核心浏览闭环

- [x] 多标签预览
  - 支持同时打开多个文件。
  - [x] 支持关闭单个标签、关闭其他标签、恢复最近关闭标签。
  - 目标：减少来回切换文件的成本。

- [ ] 前进 / 后退导航
  - 记录用户打开历史。
  - 支持回到上一个文件和前一个浏览位置。
  - 目标：让 Peek 更像浏览器，而不是一次性查看器。

- [ ] 文件内搜索
  - 支持 `Cmd/Ctrl + F`。
  - 文本、日志、HTML 源码、JSON 都应可搜索。
  - 目标：从“能看”提升到“能快速定位内容”。

- [ ] 面包屑和快速路径操作
  - 显示当前文件所在目录层级。
  - 每一层都可以点击跳转或复制路径。
  - 目标：降低深层目录浏览成本。

## P1: 提升预览深度

- [ ] 文本/代码预览增强
  - 增加语法高亮。
  - 增加跳转到指定行。
  - 增加长行换行与横向滚动切换。
  - 目标：代码文件不仅能看，还要足够顺手。

- [ ] JSON 预览增强
  - 真正实现折叠树。
  - 支持节点展开/收起、复制值、路径复制。
  - 支持错误定位，而不是只提示“无效 JSON”。

- [ ] Markdown 预览增强
  - 增加文档目录。
  - 支持标题锚点跳转。
  - 支持代码块复制。
  - 目标：让 Markdown 阅读体验明显优于普通编辑器只读模式。

- [ ] 日志预览增强
  - 支持关键字过滤。
  - 支持仅看 `ERROR/WARN`。
  - 支持自动滚动到底部。
  - 后续支持 `tail -f` 实时追踪。
  - 目标：把日志场景做成一个独立亮点。

## P2: 解决性能和大文件问题

- [x] 文件夹树改为懒加载
  - 不再递归一次性扫完整个目录。
  - 展开目录时再读取子节点。
  - 目标：大型项目打开速度稳定。

- [x] 大文件保护机制
  - 真正接入文件大小判断。
  - 超过阈值时进入“大文件模式”。
  - 大文件模式至少包含：延迟渲染、只读提示、关闭部分重型功能。

- [x] 虚拟滚动
  - 文本、日志、JSON 源码视图优先支持。
  - 目标：万行以上内容仍然流畅。

- [ ] 渐进式读取
  - 对超大文本文件分段加载。
  - 避免一次性把全部内容塞进前端内存。

## P2: 扩展高频格式边界

- [x] 图片预览
  - 支持 PNG、JPG、GIF、WebP、SVG。
  - 至少提供缩放、适应窗口、原始尺寸切换。

- [x] PDF 预览
  - 支持页码跳转、缩放、复制文本。
  - 如果暂时做不到完整能力，也可以先接系统预览兜底。

- [x] CSV 预览
  - 表格视图。
  - 首行表头识别。
  - 基础排序和搜索。

- [ ] YAML / XML
  - 目标不是“能显示字符串”，而是结构化可读。

## P3: 增加产品辨识度

- [ ] Quick Look / 系统集成
  - 尤其是 macOS 场景。
  - 目标：让 Peek 成为用户系统层面的预览习惯。

- [ ] 设为默认打开某些文本格式
  - 先从 Markdown、log、json 等场景切入。

- [ ] 分享与导出
  - 导出为截图、复制渲染结果、复制当前片段链接。
  - 仅在核心浏览链路成熟后再做。

## 暂不优先

- [ ] 插件系统
  - 当前核心体验还没打透，过早开放扩展意义不大。

- [ ] 低频文件格式的大量堆叠
  - 先做图片、PDF、CSV 这种高频需求。

- [ ] 复杂协作能力
  - 例如云同步、多人批注、远程共享等。
  - 当前阶段会显著分散产品焦点。

## 推荐版本节奏

### v1.1

- [ ] 定位文案统一
- [ ] 空状态加入最近打开
- [ ] 恢复上次工作区
- [ ] 状态持久化
- [ ] CLI 打开能力

### v1.2

- [x] 多标签
- [ ] 前进 / 后退
- [ ] 文件内搜索
- [ ] 文本语法高亮
- [ ] JSON 折叠树
- [ ] Markdown 目录
- [ ] 日志过滤

### v1.3

- [x] 懒加载文件树
- [x] 大文件模式
- [x] 虚拟滚动
- [x] 图片预览
- [x] PDF 预览
- [x] CSV 预览

## 如果只能先做 3 件事

- [ ] 最近打开 + 恢复上次工作区
- [ ] 前进 / 后退（多标签已完成）
- [ ] 文件内搜索

这 3 件事对日常打开频率、停留时长、替代编辑器只读模式的能力提升最大。
